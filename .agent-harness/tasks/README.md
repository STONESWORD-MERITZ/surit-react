# Task Queue

Create one file per task using the task prefix `SURIT`.

Current owner for new tasks: Codex.

Recommended names:

- `SURIT-001-short-title.md`
- `SURIT-002-short-title.md`

Use `TEMPLATE.md` as the starting point.

Historical task files may mention earlier Claude/Codex handoffs. Those entries are archival only; new task work should use the Codex-only template.
